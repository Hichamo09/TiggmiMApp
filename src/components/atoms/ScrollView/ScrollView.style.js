import { StyleSheet } from '../index';

const styles = StyleSheet.generate(() => ({
  style: {
    backgroundColor: 'white'
  },
  contentContainer: {
    backgroundColor: 'white'
  }

}));

export default styles;
